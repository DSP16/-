<Window x:Class="ИП_Хевеши.UI.Winds.ReportsWn"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ИП_Хевеши.UI.Winds"
        WindowStartupLocation="CenterScreen"
         Icon="pack://application:,,,/Resources/AppLogoKPIcon.ico"
        mc:Ignorable="d"
        Title="ИП Хевеши | Формирование отчета" Height="450" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
           
        </Grid.RowDefinitions>

        <Label Grid.Row="0" FontWeight="SemiBold" Content="Выберите месяц и год:" Margin="0,0,0,5" />
        <Calendar ToolTip="Календарь" Grid.Row="1" x:Name="ReportCalendar" SelectionMode="SingleDate" />

        <Label Grid.Row="2" FontWeight="SemiBold" Content="Тип отчета:" Margin="0,10,0,0" />
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <RadioButton x:Name="ArrivalsRadioButton" FontWeight="SemiBold" Content="Поступления" Margin="5" IsChecked="True" />
            <RadioButton x:Name="IssuanceRadioButton" FontWeight="SemiBold"  Content="Расходы" Margin="5" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Center">
            <Button x:Name="btnCancel" Width="200" Style="{StaticResource AutoWideButtons}" Grid.Row="4" Content="Отмена" Click="btnCancel_Click" Margin="0,10,0,0" Padding="5" />
            <Button x:Name="btnGenerateReport" Width="200" Style="{StaticResource AutoWideButtons}" Grid.Row="4" Content="Сформировать отчет" Click="btnGenerateReport_Click" Margin="10,10,0,0" Padding="5" />

        </StackPanel>

        <ProgressBar Grid.Row="5" x:Name="ReportProgressBar" IsIndeterminate="True" Visibility="Collapsed" Margin="0,10,0,0" />
        <TextBlock Grid.Row="6" x:Name="StatusTextBlock" Visibility="Collapsed" Margin="0,5,0,0" />

        

    </Grid>

</Window>
